#pragma once
#include <stdint.h>

namespace uuid {

// represents a unique identifier for a player.
typedef uint16_t player_uuid_t;

// represents a unique game world object.
typedef uint32_t object_uuid_t;

} // namespace uuid
